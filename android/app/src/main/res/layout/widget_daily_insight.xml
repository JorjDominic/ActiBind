<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_accent_background"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="✦ DAILY INSIGHT"
        android:textColor="#5B5CE2"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/insight_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:maxLines="5"
        android:text="Generate an insight in ActiBind to see it here."
        android:textColor="#172033"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/insight_updated"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="No insight synced yet"
        android:textColor="#667085"
        android:textSize="10sp" />
</LinearLayout>
